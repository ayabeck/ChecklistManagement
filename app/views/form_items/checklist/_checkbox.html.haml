-#チェックボックス
.form-group-input
  .col-sm-offset-4.col-sm-8
    .media.icon-checkbox
      .media-left.media-middle.checkbox
        %label
          = check_box_tag "form[#{ item.id }]", 'true', item.checked?
          %span
      .media-body.media-middle
        = auto_link_text(item.label)
